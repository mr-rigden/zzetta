<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"> 
    {% for page in site.pages %}
        <url>
            <loc>{{site.config.base_url}}/{{page.header.slug}}</loc>
            <lastmod>{{ page.header.datetime.strftime('%Y-%m-%d') }}</lastmod>
        </url>
    {% endfor %}
</urlset>